apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-vue-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: go-vue
  template:
    metadata:
      labels:
        app: go-vue
    spec:
      containers:
        - name: go-vue-container
          image: go-vue-app:v1
          ports:
            - containerPort: 8080
          env:
            - name: REDIS_ADDR
              value: "redis:6379"